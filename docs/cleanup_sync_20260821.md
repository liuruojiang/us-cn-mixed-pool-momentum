# 历史研究清理与远端同步记录（2026-08-21）

## 范围

- 同步此前尚未进入远端的 Sub-D 动态创业板代理研究源码、分层记录、精简指标和正式输出。
- 清理 16 个参数扫描目录下的 `daily_outputs/`，合计 16 个可再生逐日曲线文件、1,491,622,021 字节。
- 保留每次扫描的 `record.md`、`scan_summary.csv`、`window_metrics.csv`、`sources.csv`、参数选择表、元数据、命令日志和复现脚本。

## 清理判断

被清理文件均为扫描过程生成的宽表逐日曲线，不是冻结规格、结论记录或唯一输入。对应运行目录均已通过完整性检查：16/16 有 `record.md`，16/16 有扫描或比较摘要，其余审计文件仍保留。

## 防止复发

`.gitignore` 新增 `quant_param_scan_runs/**/daily_outputs/`。后续逐日宽表留在本地运行环境，GitHub 只保存足以审阅结论和复现测试的精简证据。

## 研究权限

本次仅整理研究证据并同步源码，不构成任何策略的实盘晋升或自动下单授权。
