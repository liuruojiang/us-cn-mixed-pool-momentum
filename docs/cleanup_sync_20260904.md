# 2026-09-04 测试产物清理与云端同步

## 清理范围与恢复

仅移出活动目录中可再生成的缓存和内容完全相同的重复图片：21 个缓存目录、10 张重复 PNG，共 5,553,963 字节。逐项清单见 [manifest](cleanup_sync_20260904_manifest.json)。图片按 SHA-256 判重，每组保留最新的一张；网络验收图片、文字及 CSV 未删除。

所有清理目标已先备份，再可恢复地移到 `.codex_backups/20260904_140001/removed/`，原相对路径保持不变。需要恢复时从该目录按清单移回，先检查是否已有新生成的同名文件。备份目录只留本机，不上传 GitHub；本次不是永久删除，因此不宣称释放相应磁盘空间。

## 保留内容

- 六 ETF 朴素版 V1.3 脚本、有效回归测试、冻结行情、最终曲线、指标、参数决策及 Poe 本地运行证据。
- 原六 ETF V1.1、旧混合代理池 V1.3 及原研究引擎，代码保持不变。
- 9 月 3—4 日扫描程序、元数据、汇总、校验和图表，以及比较所需的 18 份基准/已选日度曲线。其他候选日度曲线仍保留本地并由既有忽略规则排除；完整扫描校验需先依次重跑相应扫描以再生成候选曲线。
- 历史研究记录、原始输入、行情缓存和既有备份不在清理范围。

## 文档与可复现性

README、TASK_STATE 和 AGENTS 明确区分两个不同的 V1.3，避免误用旧混合池脚本。新 V1.3 冻结数据测试及 Score 上限 5.5 比较入口改为从当前检出目录定位输入；历史元数据仍保留当时的路径和哈希。未改策略参数或交易逻辑，也未修改用户记忆文件。

## 验证

清理前全量回归：566 passed、1 skipped、1 warning（46.74 秒），记录为 `outputs/subd_six_etf_v1_3_acceptance_20260904/pytest_cleanup.xml`。跳过项为需显式开启的联网测试；此前真实联网 Poe 本地运行七类查询均已通过。warning 为第三方 Pydantic 配置弃用提示。

本轮不刷新回测或重做参数选择。V1.3 脚本 SHA-256 保持 `21a5867f51367d215a6f9913dafae1a462bf2316f7c59e7a860d43448c63ee72`。

另从 Git 暂存区导出 52 个必要文件到独立检出目录，重跑 V1.3 冻结面板一致性、Poe 本地运行与安全检查：58 passed、1 skipped、1 warning（24.23 秒），确认这些测试不依赖原工作目录。提交前空白检查通过，31 个清理目标均已移出活动目录，未检出超过 50 MB 的新增文件或指定密钥模式。

## 云端目标与边界

同步目标为 GitHub 仓库 `liuruojiang/us-cn-mixed-pool-momentum` 的已有分支 `codex/previous-research-sync-20260821`。使用普通提交及非强制推送，不合并 main，不部署 Poe 托管机器人，不触发实盘交易。实际提交及推送完成状态以 Git 记录和远端分支 SHA 核验为准。
